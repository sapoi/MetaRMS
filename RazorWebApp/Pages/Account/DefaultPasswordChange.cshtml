<!-- @page "{handler?}"
@using SharedLibrary.Enums
@model RazorWebApp.Pages.Account.DefaultPasswordChangeModel
@{ 
    ViewData["Title"] = "Change default password"; 
}

<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand">@Model.MenuData.AppName</a>
        </div>
    </div>
</nav>


<div class="row">
    <div class="col-sm-12">
        <h2>@ViewData["Title"]</h2>
        @if (Model.Message != null || Model.Message != ""){
                <div class="modal-body col-sm-12 center-block text-center">
                    <span><b>@Model.Message</b></span>
                </div>
        }
        <form method="post">
            <div class="form-group">
                <label>New password</label>
                <small>@Model.ApplicationDescriptor.SystemDatasets.UsersDatasetDescriptor.PasswordAttribute.Description</small>
                <input name="ValueList[0]" class="form-control" type="password" required>
            </div>
            <div class="form-group">
                <label>New password once again</label>
                <input name="ValueList[1]" class="form-control" type="password" required>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-default">Change</button>
            </div>
        </form>
    </div>
</div> -->